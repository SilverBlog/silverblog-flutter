name: demoBlog
description: a Demo Blog Writen by Flutter.

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^1.0.0
  scoped_model: ^1.1.0
  flutter_ckb_localization: ^0.0.6
  flutter_screenutil: ^1.0.2
  device_info: ^0.4.2+4

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: false
